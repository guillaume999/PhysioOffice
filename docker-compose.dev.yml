version: "3.8"
services:
  physiooffice-dev:
    build:
      context: .
      dockerfile: Dockerfile
    image: patient-hub-frontend-physiooffice:dev
    # Build the image locally instead of pulling it from a registry.
    # This stack has no registry image, so `docker compose pull` would fail
    # with "pull access denied for patient-hub-frontend-physiooffi